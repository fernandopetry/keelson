# keelson.config.json (trecho)

```json
{
  "docsRoot": "docs",
  "quality": { "test": "vendor/bin/phpunit", "lint": "vendor/bin/phpcs" },
  "gates": { "security": true, "screenVerify": true },
  "git": { "branchNaming": "default" }
}
```

Ambiente desta sessão: working tree única (nenhum worktree por task foi criado);
modo de orquestração padrão.
