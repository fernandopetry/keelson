# Doutrina de comentários (braço MÍNIMO — controle "regra × default do modelo")

> Braço de controle (4.304, "poda só com prova"): **zero doutrina de comentário**. Mede
> quanto o modelo comenta por default — sem ele, ART7 × reescrita compara duas réguas e
> não diz se alguma delas muda o comportamento (lacuna da 1ª rodada, 4.420).

Não há regra de comentário neste projeto. Escreva o código como você escreveria.
