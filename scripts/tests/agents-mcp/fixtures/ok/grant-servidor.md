---
name: grant-servidor
description: Fixture válida — grant no nível do servidor cobre qualquer ferramenta dele.
tools: Read, Grep, mcp__foo
---

# Subagent: grant-servidor

Dirija o serviço com as ferramentas `mcp__foo__bar` e `mcp__foo__baz`.
