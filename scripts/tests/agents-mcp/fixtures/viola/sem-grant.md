---
name: sem-grant
description: Fixture violadora — corpo cita dois servidores MCP sem grant no tools.
tools: Read, Bash, Glob, Grep
model: sonnet
---

# Subagent: sem-grant

Sonde se as ferramentas `mcp__foo__bar` respondem antes do fluxo de tela.
Depois consulte `mcp__multi_word__fetch` para o payload.
