# acoes_sugeridas — code-reviewer, wave 1 (Art. 7: comentário que repete o código)

Remoções sugeridas (não bloqueiam; aplicar neste retry, delta inerte):

1. `src/Faturamento/CalculadoraService.php:17` — `// soma os itens`
2. `src/Faturamento/CalculadoraService.php:20` — `// acumula o preço do item vezes a quantidade`
3. `src/Faturamento/CalculadoraService.php:27` — `// retorna o bruto`

Os demais comentários do arquivo (limiar/DEC-004-002, docblock com a regra de negócio,
`// aplica o desconto se passou do limiar`) **não** estão na lista.
