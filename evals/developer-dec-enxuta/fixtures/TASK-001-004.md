# TASK-001-004: Reenviar ao parceiro um pedido pendente de envio

**Slug**: pedidos
**Pertence a**: PLAN-001
**Realiza (FRs)**: FR-001-005
**Componente**: COMP-001-003 (principal)
**Wave**: 3
**Tamanho estimado**: small
**Tipo**: feature
**Status**: Todo

## Dependências

- **Depende de**: TASK-001-002
- **Bloqueia**: nenhuma

## Contexto

Reenvio manual como nova ação sobre pedido `pendente` — território em `MEMO.md`; decisões
que tocam o escopo: DEC-001-002, DEC-001-003, DEC-001-004, DEC-001-005 (no PLAN).

## Escopo

### Inclui
- `src/Pedidos/ConfirmacaoService.php`: método `reenviar(int $id, Operador $op): void`.
- `src/Http/PedidoController.php`: ação `reenviar()` na rota `POST /pedidos/{id}/reenviar`.
- `tests/Pedidos/ConfirmacaoServiceTest.php`.

### Não inclui
- Reenvio automático/agendado.
- Tela (botão de reenvio é da TASK-001-005).

## Critérios de pronto

- [ ] Testes cobrem AC-001-004 — Dado um pedido `pendente` do operador logado, quando ele aciona o reenvio e o parceiro responde sucesso com `desconto_pct` 8, então `envio_status = enviado` e `desconto_frete_pct = 8.00`. Verificação: `vendor/bin/phpunit --filter ConfirmacaoServiceTest::testReenviaPendente` → `OK (1 test)`.
- [ ] Reenvio de pedido que **não** está `pendente` é recusado sem chamar o parceiro (teste próprio).
- [ ] Reenvio por operador que não pode reenviar aquele pedido é recusado e nada muda (teste próprio) — ver DECs do PLAN para a regra e a camada.
- [ ] Falha do parceiro no reenvio segue a DEC-001-005 (teste próprio).
- [ ] Sem warnings/lints novos sobre todos os arquivos do diff (`git diff --name-only main...HEAD`).

## Riscos específicos

- nenhum além dos TRISK do PLAN.

---

## Histórico de execução (preenchido pelo /keelson:implement)

<!-- /keelson:implement preenche durante closure. Não editar manualmente. -->
